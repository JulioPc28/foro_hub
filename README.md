# Foro Hub 📚💻

API REST desarrollada con **Spring Boot** para gestionar un foro de discusión.  
Permite a los usuarios crear, listar, actualizar y eliminar tópicos, siguiendo las mejores prácticas de arquitectura REST.

## 🚀 Funcionalidades
- Crear un nuevo tópico (`POST /topicos`)
- Listar todos los tópicos (`GET /topicos`)
- Consultar un tópico específico (`GET /topicos/{id}`)
- Actualizar un tópico (`PUT /topicos/{id}`)
- Eliminar un tópico (`DELETE /topicos/{id}`)

## 🛠️ Tecnologías
- Java 17
- Spring Boot 3
- Spring Web
- Spring Data JPA
- H2 Database (para pruebas)

## ▶️ Ejecución
1. Clonar el repositorio:
   ```bash
   git clone https://github.com/tuusuario/foro-hub.git
